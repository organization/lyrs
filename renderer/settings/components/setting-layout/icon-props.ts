import { vars } from '@suis-ui/kit/css';

export const iconSmallProps = {
  color: vars.color.text.main,
  size: 16,
} as const;

export const iconMediumProps = {
  color: vars.color.text.main,
  size: 24,
} as const;

export const iconSuccessProps = {
  color: vars.color.success.main,
  size: 24,
} as const;

export const iconErrorProps = {
  color: vars.color.error.main,
  size: 24,
} as const;
